## 将本文件复制为 `.env.local` 并填入你的 Key
## 本文件仅示例：不要把真实 Key 提交到仓库

# 推荐（更安全）：仅给 dev server 代理使用，不会被注入到前端代码中
AI_API_KEY=your_api_key_here

# 兼容：如果你已经在用 VITE_ 前缀也可以（但它会被注入到前端，生产环境不建议）
VITE_AI_API_KEY=your_api_key_here

# 可选：兼容 OpenAI/第三方 OpenAI-style 接口
AI_BASE_URL=https://api.openai.com
VITE_AI_BASE_URL=https://api.openai.com

# 可选：模型名（按你的服务商支持填写）
VITE_AI_MODEL=gpt-4o-mini

